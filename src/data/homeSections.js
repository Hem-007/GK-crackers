/*
=====================================================
HOME PAGE SPECIAL SECTIONS DATA
=====================================================
*/

import products from "./products";

// Example logic (can be changed later)
export const bestSellers = products.slice(0, 8);
export const limitedSales = products.slice(8, 16);
